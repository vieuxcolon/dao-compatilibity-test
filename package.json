{
  "name": "dao-investment-dapp-backend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "ethers": "6.10.0",
    "dotenv": "16.1.0"
  },
  "devDependencies": {
    "hardhat": "3.1.4",
    "@nomicfoundation/hardhat-toolbox": "3.1.4"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js"
  },
  "engines": {
    "node": ">=22.10.0"
  }
}
